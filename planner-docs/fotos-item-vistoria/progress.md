# Progresso — fotos-item-vistoria

- [x] 01-model-foto-item-vistoria — validado (model `FotoItemVistoria` + migration `0012_fotoitemvistoria`; `migrate` e `check` OK)
- [x] 02-form-widget-multiplo-arquivo — validado (`MultiplaImagemInput`/`MultiplaImagemField` implementados; `ItemVistoriaForm.fotos` exposto; formset intacto)
- [x] 03-views-laudo — validado (`_salvar_fotos_itens` grava fotos após `item_formset.save()` em create/edit; `request.FILES` passado ao formset; prefetch `itens__fotos` só em `laudo_detail`, não em `laudo_gerar_pdf`; teste integrado confirma `item_id` correto, path `itens_vistoria/{item_id}/`, skip/DELETE ignorados, CASCADE; 118 testes OK)
- [x] 04-templates-laudo — validado (`laudo_form.html`: `enctype="multipart/form-data"`, coluna "Fotos" com `f.fotos` no checklist; `laudo_detail.html`: galeria de `item.fotos.all` abaixo da linha do item, só quando houver fotos; `laudo_pdf.html`/`documentos.html` intocados; `check` e suíte completa — 118 testes — OK; validação visual em claro/escuro não realizada nesta janela por falta de ferramenta de browser)
- [x] 05-tests — validado (classe `FotoItemVistoriaTests` com os 7 casos do módulo; corrigido bug real encontrado pelo teste 3: `MultiplaImagemField` não sobrescrevia `has_changed`, e como `ClearableFileInput.value_from_datadict` devolve `[]` — não `None` — para linhas sem arquivo, `FileField.has_changed` (`data is not None`) marcava **toda** linha do formset como alterada, quebrando a regra "linha vazia é ignorada" (`CLAUDE.md`); adicionado `has_changed` em `MultiplaImagemField` (`imoveis/forms.py`) tratando `data` falsy como não alterado; suíte completa — 125 testes — OK; `check` OK)
